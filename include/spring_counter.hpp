#ifndef SPRING_COUNTER_H_
#define SPRING_COUNTER_H_

#include "mesh.h"
#include <vector>

std::vector<int> count_spring(const SimpleMesh& mesh);

std::vector<int> count_spring();

#endif // SPRING_COUNTER_H_
